<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.coinapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name="com.example.coinapp.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoinApp"
        tools:targetApi="31">
        <service
            android:name=".ui.floating.FloatingWindowService"
            android:exported="false" />

        <activity
            android:name=".ui.setting.floatingwindow.FloatingWindowSettingActivity"
            android:exported="false" />
        <activity
            android:name=".ui.setting.floatingwindow.FloatingTickerSelectActivity"
            android:exported="false" />
        <activity
            android:name="com.example.coinapp.ui.home.detail.TickerDetailActivity"
            android:exported="false" />
        <activity
            android:name="com.example.coinapp.ui.main.MainActivity"
            android:exported="false" />
        <activity
            android:name="com.example.coinapp.ui.splash.InitActivity"
            android:exported="true"
            android:theme="@style/Theme.CoinApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>